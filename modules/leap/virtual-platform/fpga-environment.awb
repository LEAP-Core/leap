%name FPGA Environment
%desc FPGA Environment

%provides fpgaenv

%requires virtual_platform
%requires fpga_components
%requires soft_services
%requires librl_bsv
%requires build_pipeline

%notes README

%attributes leap

%param --global  BUILD_PLATFORM_MODULES 1 "Actually instantiate platform"


