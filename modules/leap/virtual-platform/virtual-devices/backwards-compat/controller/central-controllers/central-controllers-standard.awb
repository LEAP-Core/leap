%name Standard Central Controllers
%desc Standard Central Controllers Grouping
%provides central_controllers

%attributes hasim

%requires module_controller
%requires events_controller
%requires stats_controller
%requires debug_scan_controller
%requires assertions_controller
%requires params_controller

%sources -v PUBLIC  central-controllers-standard.h
%sources -v PRIVATE central-controllers-standard.cpp
%sources -v PUBLIC  central-controllers-standard.bsv
