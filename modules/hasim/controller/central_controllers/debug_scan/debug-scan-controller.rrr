service DEBUG_SCAN
{
    server sw (cpp, method) <- hw (bsv, connection)
    {
        method Send(in UINT32[32] debugScanID, in UINT8[8] value);
        method Done(in UINT8[8] syn, out UINT8[8] ack);
    };
};
